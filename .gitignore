# Credentials — never commit these
.env
.env.local
.env.*.local

# Claude Code local settings (may contain API keys or env vars)
.claude/settings.local.json
settings.local.json

# macOS
.DS_Store

# Local scratch files
*.log
tmp/
